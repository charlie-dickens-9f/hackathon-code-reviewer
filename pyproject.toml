[project]
name = "ai-pr-reviewer"
version = "0.1.0"
description = "AI-powered code review for GitHub pull requests"
requires-python = ">=3.13"

dependencies = [
    "openai>=1.3.0",
    "requests>=2.31.0",
]

